name: Add git

on: [push]

jobs:
  add-git:
    name: Add git
    runs-on: ubuntu-latest
    steps:
      
      - name: Clone git repo
        run: git clone https://${{ secrets.ACCESS_TOKEN }}@github.com/arachna-net/sandbox.git